name: grains
version: 1.2.0.6

dependencies:
  - base

library:
  exposed-modules: Grains
  source-dirs: src
  ghc-options: -Wall

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - grains
      - hspec
